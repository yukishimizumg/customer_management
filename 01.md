```sql
-- データベースの作成
CREATE DATABASE customer_management;
```

```sql
-- 作業ユーザーの作成とパスワードの設定
-- 今回はホストを指定しない
CREATE USER customer_admin IDENTIFIED BY '1234';
```

```sql
-- 「customer_management」というデータベースの全てのテーブルの操作権限を「customer_admin」に付与。
GRANT ALL ON customer_management.* TO customer_admin;
```

<br>

```sql
CREATE TABLE customers (
    id INT PRIMARY KEY AUTO_INCREMENT,
    company VARCHAR(255) NOT NULL,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(255) NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

```sql
INSERT INTO
    customers
    (company, name, email)
VALUES
    ('Appple Inc.', '鈴木一郎', 'suzuki@example.com'),
    ('Metflix Inc.', '佐藤二郎', 'sato@example.com'),
    ('有限会社ラーメン三郎', '石倉三郎', 'ishikura@example.com');
```

